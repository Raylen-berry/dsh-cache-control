dsh-cache-control
=================

本插件的 ponytail 段（ponytail-gate.md）蒸馏自：
    Ponytail — https://github.com/DietrichGebert/ponytail
    Copyright (c) DietrichGebert · MIT License

本插件的自动审查技能（skills/auto-code-review/SKILL.md）依据并引用：
    Open Code Review — https://github.com/alibaba/open-code-review
    Copyright (c) Alibaba · Apache License 2.0

关于自动审查的两点说明：

1. **没有把上游的规则文本抄进本仓库。** 上游 README 明确把"通用 agent + 自然语言 skill 做代码
   审查"列为反面教材（覆盖不全 / 行号漂移 / 质量不稳），其解法是确定性工程硬约束选文件、分组、
   匹配规则。抄规则文本 = 只拿走它论证过会失败的那一半。所以本技能每次运行时现向 `ocr`
   （delegation mode）取「该审哪些文件」与「这些文件命中哪些规则」，本仓库不存规则正文。
   技能正文是本项目的原创转述，其中对上游设计动机与基准数字的描述属于事实性引用。

2. **运行期依赖上游的可执行文件，不是它的源码。** 用户自行 `npm install -g
   @alibaba-group/open-code-review`（Apache-2.0，允许商用与再分发，需保留署名）。本插件不打包、
   不下载、不自动安装该 CLI；探测不到时界面如实显示"没找到 ocr"，技能跑到第一步也会停下说明。

上游许可证全文见其仓库 LICENSE（Apache-2.0）与 https://www.apache.org/licenses/LICENSE-2.0 。

本插件的「省 token」能力（save-token-host.js 与 save-token-core.js）嵌入自：
    dsh-plugin-save-token — https://github.com/vibe-any/dsh-plugin-save-token
    Copyright (c) playwithai / vibe-any · MIT License
    上游版本 v2.4.1，源文件 src/index.js（host 半）与 src/compress.js（纯函数核）

关于这次嵌入的三点说明：

1. **改动范围有据可查。** 两个文件都带 `VENDORED INTO dsh-cache-control` 文件头，逐条列了相对
   上游删掉了什么。唯一一处功能删减是上游的 compaction assist（上游 "arm 3"：`agent/pre-step`
   压力触发、水位线、冷却期、`compactStats`、`ctx.get('compaction')`）—— 它和本插件自己的
   「省缓存」抢同一个压缩引擎，两套阈值同时改写同一个 preset 行必然互相打架，所以整段删除而不是
   默认关掉（默认关掉只是把冲突藏起来）。压缩契约（触发点 / 尾部保留 / 自动压缩）仍只由本插件持有。
2. **没有装上游的 composer.dock 常驻小条。** 那个槽位由 dsh-bill 占用（它在同一位置画每会话费用
   行），同一格塞两块 UI 会互相挤掉。统计仍在，只是改由设置页的「省 token」卡呈现。
3. **统计只留计数与字节数。** 与上游一致，不落盘任何 prompt 文本；计数与两个开关都在内存里，
   重启归零、回到默认开。

上游许可证全文见其仓库 LICENSE（MIT）。
